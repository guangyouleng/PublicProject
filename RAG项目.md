技术栈

#### 1. 数据处理与接入层：“为什么要用 PaddleOCR 和 Python？”

- **话术示例**：“在真实的企业场景中，很多知识沉淀在扫描件 PDF 或图片里。为了把这些非结构化数据变成机器可读的文本，我选择了 **PaddleOCR**。它在中文识别上的精度非常高，而且对复杂版面（比如表格、多栏排版）的处理能力很强，能最大程度保证知识库源头的质量。而整个后端和胶水代码自然是用生态最完善的 **Python** 来实现。”

#### 2. 核心检索链路：“为什么选 BGE-M3 + Milvus + Reranker？”（这是最能体现你技术深度的地方）

- 话术示例

  ：“为了保证问答的准确率，我在检索链路做了精心的设计：

  - **Embedding 模型**：我选了 **BGE-M3**，而不是普通的 BERT。因为 M3 是目前开源界 SOTA（最先进）的多语言模型之一，它支持混合检索（稠密+稀疏），能同时兼顾语义匹配和关键词精确匹配，解决了传统向量检索‘查不准’的问题。
  - **向量数据库**：面对海量的企业文档，普通的数据库扛不住相似度搜索的压力，所以我用了专业的向量数据库 **Milvus**，它能支持亿级向量的毫秒级检索，并且方便后续做分布式扩展。
  - **重排序（Rerank）**：光有向量检索还不够，为了进一步减少大模型的幻觉，我引入了 **BGE-Reranker**。它会从 Milvus 召回的 Top-K 结果中进行二次精排，把真正最相关的片段挑出来给大模型，这一步显著提升了最终答案的质量。”

#### 3. 性能与架构优化：“为什么加 Redis 和 Docker？”

- **话术示例**：“考虑到用户体验，响应速度至关重要。我引入了 **Redis** 做两层优化：一是作为**语义缓存**，对于重复的高频提问直接返回结果，不用走昂贵的 LLM 推理；二是利用它的高速读写处理会话状态。而在部署层面，因为 AI 项目的依赖环境非常复杂（各种 CUDA、Python 库版本），使用 **Docker** 容器化交付，彻底解决了环境一致性问题，实现了快速上线和扩容。”

#### 4. 业务支撑与编排：“LangChain、Flask 和 MySQL 的作用？”

- **话术示例**：“为了让系统具备工程化的落地能力，我用 **LangChain** 来标准化地编排整个 RAG 流程（文档加载、切分、Prompt 管理等），大大降低了开发复杂度。对外通过轻量级的 **Flask** 暴露 RESTful API。**MySQL** 则负责兜底，存储用户信息、权限管理以及对话日志等结构化业务数据，保证系统的安全性和可追溯性。”