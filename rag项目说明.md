# 集成问答系统（RAGQ）— 项目文档

## 一、项目概述

本项目是一个**基于 RAG（检索增强生成）+ MySQL + Redis 的集成问答系统**，采用 FastAPI 提供流式 API 接口。系统支持多轮对话、学科过滤，并融合了两条检索链路：

- **结构化知识检索**：从 MySQL 中存储的问答对里通过 BM25 关键词匹配直接给出答案
- **知识库 RAG 检索**：当 MySQL 无可靠答案时，回退到 Milvus 向量数据库，进行混合检索 + 重排序 + LLM 生成

---

## 二、核心流程

### 2.1 整体请求链路

```
用户提问
  │
  ▼
api.py  (FastAPI POST /query, SSE 流式接口)
  │
  ▼
IntegratedQASystem.query() ──────────────────────────────────┐
  │                                                            │
  │  ① 获取对话历史 (MySQL conversations 表, 最近 5 轮)        │
  │                                                            │
  │  ② BM25 搜索 (mysql_qa/retrieval/bm25_search.py)           │
  │     ┌──────────────────────────────────────────────────┐   │
  │     │  2.1 先查 Redis 缓存 (key = "answer:{用户问题}")   │   │
  │     │           ↓ 命中 → 直接返回答案                    │   │
  │     │           ↓ 未命中                                │   │
  │     │  2.2 从 Redis 加载 BM25 索引 (原始问题 + 分词问题)  │   │
  │     │           ↓ Redis 无数据 → 回源 MySQL 拉取         │   │
  │     │  2.3 对用户问题分词 → BM25 打分 → Softmax 归一化   │   │
  │     │  2.4 最高分 > 0.85 ?                              │   │
  │     │     ┌──────┴──────┐                               │   │
  │     │    YES            NO                              │   │
  │     │     │             │                               │   │
  │     │  用最高分问题     返回 None,                       │   │
  │  查询 MySQL 答案       need_rag=True                     │   │
  │  写入 Redis 缓存       → 走 RAG 链路                     │   │
  │  直接返回答案          ↓                                 │   │
  │                     ③ RAG 检索                           │   │
  │                     ④ 流式返回答案                       │   │
  │                     ⑤ 写入 MySQL 对话历史                │   │
  │                     └──────────────────────────────────┘   │
  └────────────────────────────────────────────────────────────┘
```

### 2.2 分步详解

#### Step 1：请求入口 (`api.py`)

- FastAPI 接收 `POST /query`，请求体包含 `query`（用户问题）、`source_filter`（学科过滤，可选）、`session_id`（会话 ID，可选）
- 自动生成 UUID 作为 `session_id`（若未提供）
- 调用 `IntegratedQASystem.query()` 获取生成器，通过 **SSE (Server-Sent Events)** 逐 token 流式返回

#### Step 2：BM25 搜索 (`mysql_qa/retrieval/bm25_search.py`)

这是第一道检索链路，用于快速匹配已有标准答案。

| 环节 | 说明 |
|------|------|
| **2.1 Redis 缓存** | 先查 `answer:{用户问题}`，命中则直接返回，跳过后续所有检索 |
| **2.2 Redis 加载索引** | 从 Redis 获取 `qa_original_questions`（原始问题列表）和 `qa_tokenized_questions`（分词后问题列表） |
| **2.3 回源 MySQL** | 如果 Redis 为空，从 MySQL 拉取问答数据 → 分词 → 写回 Redis |
| **2.4 BM25 打分** | 对用户问题分词 → 计算 BM25 分数 → Softmax 归一化 → 取最高分 |
| **2.5 阈值判断** | 最高分 > 0.85 → 用该问题查询 MySQL 答案 → 写 Redis 缓存 → 返回 |
| **2.6 无可靠答案** | 最高分 <= 0.85 → 返回 None，标记 `need_rag=True`，回退到 RAG |

#### Step 3：RAG 检索 (`rag_qa/core/rag_system.py`)

当 BM25 无可靠答案时，进入 RAG 流程。

```
RAGSystem.generate_answer()
  │
  ▼
┌─────────────────────────────────────────────────────────────────┐
│  3.1 意图识别 — QueryClassifier (BERT)                           │
│      输入: 用户问题                                               │
│      输出: "通用知识" 或 "专业咨询"                                │
│                                                                  │
│      → 如果是"通用知识"：直接用 LLM 回答，不走向量检索              │
│      → 如果是"专业咨询"：继续下面的 RAG 流程                       │
└─────────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────────┐
│  3.2 策略选择 — StrategySelector (LLM)                           │
│      输入: 用户问题                                               │
│      输出: 四选一                                                 │
│        • 直接检索：意图明确，直接查向量库                          │
│        • 假设问题检索 (HyDE)：先生成假设答案，再用假设答案检索      │
│        • 子查询检索：拆分为多个子查询，分别检索后合并               │
│        • 回溯问题检索：将复杂问题简化为基础问题后再检索             │
└─────────────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────────────┐
│  3.3 混合检索 — VectorStore.hybrid_search_with_rerank()          │
│                                                                  │
│      ① 用 BGE-M3 生成查询的稠密向量 + 稀疏向量                     │
│      ② Milvus 稠密检索 (IVF_FLAT, 内积) → Top-K                   │
│      ③ Milvus 稀疏检索 (SPARSE_INVERTED_INDEX, 内积) → Top-K      │
│      ④ 加权融合 (稠密权重 1.0, 稀疏权重 0.7) → 合并 Top-K          │
│      ⑤ 子块去重 → 提取父文档                                     │
│      ⑥ BGE-Reranker-Large 重排序 → 取 Top-M (candidate_m=2)      │
└─────────────────────────────────────────────────────────────────┘
  │
  ▼
  3.4 拼接上下文 → 填入 RAG Prompt → LLM 流式生成答案
  │
  ▼
  3.5 写入 MySQL conversations 表 (保留最近 5 轮)
```

### 2.3 数据处理流程（文档入库）

当 `query_mode=False` 时，执行文档处理与向量化入库：

```
扫描 data/{学科}_data/ 目录 (PDF/PPTX/DOCX/PNG/TXT 等)
  │
  ▼
OCR 文档加载 (PaddleOCR) → 提取文本内容
  │
  ▼
父子分块 (ChineseRecursiveTextSplitter)
  ├── 父块: chunk_size=1200, overlap=50
  └── 子块: chunk_size=300, overlap=50
  │
  ▼
为子块添加元数据 (parent_id, parent_content, source, timestamp)
  │
  ▼
BGE-M3 向量化 (稠密 + 稀疏向量)
  │
  ▼
写入 Milvus (upsert 操作, MD5 哈希作为唯一 ID)
```

### 2.4 对话历史管理

- 存储在 MySQL `conversations` 表
- 字段：`id`, `session_id`, `question`, `answer`, `timestamp`
- 按 `session_id` 索引，保留最近 5 轮对话
- 新对话插入后，自动删除超出 5 轮的旧记录
- 提供 `clear_session_history()` 清空指定会话

---

## 三、技术栈与模型详细介绍

### 3.1 BM25（Best Matching 25）

**是什么**：BM25 是一种经典的基于关键词频率的全文检索算法，源自概率检索模型。它通过计算查询词与文档之间的相关性分数来排序文档。

**核心原理**：
- **词频（TF）**：查询词在文档中出现的次数，出现越多相关性越高，但有饱和上限
- **逆文档频率（IDF）**：查询词在整个语料库中的稀有程度，越稀有的词权重越大
- **文档长度归一化**：长文档不会被单纯因为长度而获得更高分数

**在项目中做了什么**：
- 用于第一道快速检索。用户问题先与 MySQL 中预存的标准问题进行关键词匹配
- 使用 `rank_bm25` 库的 `BM25Okapi` 实现
- 配合 Softmax 将原始 BM25 分数归一化为概率分布，取最高分与阈值 0.85 比较
- 命中率高时直接返回 MySQL 中的标准答案，无需调用 LLM，**大幅降低延迟和成本**

### 3.2 BGE-M3（BAAI General Embedding - M3）

**是什么**：由北京智源人工智能研究院（BAAI）开发的多功能嵌入模型，单模型同时支持**稠密检索、稀疏检索和多向量检索**三种模式，支持 100+ 种语言，最大输入长度 8192。

**核心特性**：
- **多粒度**：支持词级、句子级、文档级嵌入
- **多语言**：中文、英文等 100+ 语言
- **多功能**：同时输出稠密向量（Dense）和稀疏向量（Sparse）

**在项目中做了什么**：
- 作为 Milvus 的嵌入模型，将文档块和用户问题转换为向量
- 同时生成**稠密向量**（1024 维，用于语义相似度匹配）和**稀疏向量**（用于关键词精确匹配）
- 在项目中本地部署（`rag_qa/models/bge-m3/`），不依赖外部 API

### 3.3 BGE-Reranker-Large

**是什么**：同样来自 BAAI 的交叉编码器（Cross-Encoder）重排序模型。与嵌入模型不同，Cross-Encoder 会将 query 和 document 拼接后一起输入模型，直接输出相关性分数，精度更高但计算量更大。

**在项目中做了什么**：
- 用于 RAG 检索的**最后一步精排**
- 在 Milvus 混合检索返回 Top-K 个候选文档后，将 `(query, doc)` 对输入 Reranker 重新打分
- 按得分从高到低排序，取 Top-2（`candidate_m=2`）作为最终上下文
- **作用**：弥补向量检索的精度不足，确保送给 LLM 的上下文质量最高
- 本地部署（`rag_qa/models/bge-reranker-large/`）

### 3.4 Milvus（向量数据库）

**是什么**：开源的分布式向量数据库，专为 AI 场景设计，用于存储、索引和查询大规模向量数据。支持多种索引类型（IVF_FLAT、HNSW 等）和混合检索。

**在项目中做了什么**：
- 存储文档的**稠密向量 + 稀疏向量**以及元数据（原文、父块内容、学科、时间戳等）
- 集合 Schema 包含 7 个字段：`id`, `text`, `dense_vector`, `sparse_vector`, `parent_id`, `parent_content`, `source`, `timestamp`
- 稠密向量使用 **IVF_FLAT** 索引（128 个簇），稀疏向量使用 **SPARSE_INVERTED_INDEX** 索引（20% 降噪）
- 支持**混合检索（hybrid_search）**：稠密 + 稀疏加权融合
- 支持按 `source` 字段过滤（学科过滤）

### 3.5 BERT 查询分类器（QueryClassifier）

**是什么**：基于 `bert-base-chinese` 微调的文本分类模型，将用户问题分为两类：**通用知识**（直接问 LLM）或**专业咨询**（走 RAG 检索）。

**在项目中做了什么**：
- 用户问题的**第一层判断**，决定是否需要检索知识库
- 如果分类为"通用知识"（如"今天天气如何"），直接让 LLM 回答，跳过向量检索
- 如果分类为"专业咨询"，进入 RAG 检索流程
- **训练数据**：`model_generic_2000.json`（2000 条标注样本）
- **作用**：减少不必要的向量检索，提升响应速度，降低系统开销

### 3.6 Redis（缓存）

**是什么**：高性能的内存键值存储数据库，常用于缓存、会话管理等场景。

**在项目中做了什么**：
| 用途 | Key | Value |
|------|-----|-------|
| 答案缓存 | `answer:{用户问题}` | 标准答案文本 |
| 原始问题索引 | `qa_original_questions` | 问题列表（供 BM25 返回答案用） |
| 分词问题索引 | `qa_tokenized_questions` | 分词后的问题列表（供 BM25 打分用） |

**缓存策略**：
- BM25 搜索时先查 `answer:{query}`，命中则直接返回
- 初始化时从 Redis 加载 BM25 索引，若为空则回源 MySQL 拉取并写入 Redis
- **效果**：首次访问从 MySQL 加载，后续全部在内存中完成，速度大幅提升

### 3.7 MySQL（关系型数据库）

**在项目中做了什么**：
- 存储**学科问答对**（BM25 检索的数据源）
- 存储**对话历史**（`conversations` 表，支持多轮对话）

### 3.8 DashScope / 通义千问（LLM）

**是什么**：阿里云的通义千问大模型 API，通过 OpenAI 兼容接口调用。

**在项目中做了什么**：
- **策略选择**：LLM 分析用户问题，选择最佳检索策略
- **假设文档生成（HyDE）**：生成假设答案，用假设答案检索
- **子查询拆分**：将复杂问题拆为多个子查询
- **问题回溯简化**：将复杂问题简化为基础问题
- **最终答案生成**：将检索到的上下文 + 用户问题填入 Prompt，流式生成答案
- 配置的模型：`qwen3-max`（`config.ini`）

### 3.9 FastAPI + SSE（流式 API）

**是什么**：FastAPI 是 Python 的高性能 Web 框架，SSE（Server-Sent Events）是 HTTP 长连接协议，支持服务器向客户端推送实时数据流。

**在项目中做了什么**：
- `/query` 接口返回 `StreamingResponse`，媒体类型 `text/event-stream`
- 每个 token 以 `data: {"token": "...", "is_complete": false, "session_id": "..."}` 格式推送
- 前端可实时逐字显示 LLM 生成的答案

### 3.10 PaddleOCR（文档解析）

**是什么**：百度开源的 OCR（光学字符识别）引擎，支持多语言文本检测与识别。

**在项目中做了什么**：
- 在文档入库时，用于解析**图片（PNG/JPG）、PDF、PPTX、DOCX**等格式
- 自定义加载器（`edu_pdfloader`, `edu_pptloader`, `edu_imgloader`, `edu_docloader`）基于 PaddleOCR 提取文本
- 使得非结构化文档能够被向量化并入库检索

### 3.11 父子分块策略

**是什么**：一种文档切分策略，先将文档切为较大的"父块"，再将每个父块切为较小的"子块"。检索时返回子块，但送入 LLM 的上下文使用对应的父块内容。

**在项目中做了什么**：
- **父块**：chunk_size=1200，保留完整语义上下文
- **子块**：chunk_size=300，粒度更细，检索精度更高
- 子块携带 `parent_id` 和 `parent_content` 元数据
- 检索到子块后，通过 `parent_content` 去重，将完整父块作为 LLM 上下文
- **优势**：子块检索精准 + 父块上下文完整，兼顾检索精度和生成质量

### 3.12 检索策略选择器（StrategySelector）

LLM 驱动的动态策略选择，根据问题特征选择最优检索方式：

| 策略 | 适用场景 | 示例 |
|------|----------|------|
| **直接检索** | 意图明确，查询特定信息 | "AI学科学费是多少？" |
| **假设问题检索 (HyDE)** | 抽象问题，直接检索效果差 | "人工智能在教育领域的应用有哪些？" |
| **子查询检索** | 涉及多个方面需拆分 | "比较 Milvus 和 Zilliz Cloud 的优缺点" |
| **回溯问题检索** | 复杂问题需简化 | "100亿条数据能存入Milvus吗？" |

---

## 四、项目启动方式

### 4.1 依赖服务

| 服务 | 地址 | 说明 |
|------|------|------|
| MySQL | localhost:3306 | 存储问答对和对话历史 |
| Redis | localhost:6379 | BM25 索引缓存 + 答案缓存 |
| Milvus | localhost:19530 | 向量数据库 |

### 4.2 配置文件

`config.ini` 包含所有参数配置，主要修改项：

```ini
[mysql]       — 数据库连接
[redis]       — Redis 连接
[milvus]      — 向量库连接
[llm]         — DashScope API Key 和模型
[retrieval]   — 分块大小和检索参数
[app]         — 学科列表和客服电话
```

### 4.3 启动方式

| 方式 | 命令 | 说明 |
|------|------|------|
| **API 服务** | `python api.py` | 启动 FastAPI，端口 8000 |
| **命令行交互** | `python new_main.py` | 终端问答 |
| **纯 RAG 查询** | `python rag_qa/rag_main.py` | 仅 RAG 模式 |
| **纯 MySQL QA** | `python mysql_qa/main.py` | 仅 BM25 模式 |
| **文档入库** | `rag_main.py` 中设置 `query_mode=False` | 向量化文档 |

---

## 五、系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户 (HTTP POST /query)                   │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     FastAPI (api.py)                             │
│                   SSE 流式响应接口                                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│               IntegratedQASystem (new_main.py)                   │
│                                                                  │
│  ┌─────────────────────┐      ┌──────────────────────────────┐  │
│  │  BM25Search         │  NO  │  RAGSystem                   │  │
│  │  ┌───────────────┐  │ ────▶│  ┌────────────────────────┐  │  │
│  │  │ Redis 缓存检查 │  │      │  │ QueryClassifier (BERT) │  │  │
│  │  └───────┬───────┘  │      │  │   通用知识→直接LLM      │  │  │
│  │          │ YES      │      │  │   专业咨询→继续RAG      │  │  │
│  │          ▼          │      │  └────────────┬───────────┘  │  │
│  │  BM25 打分+阈值判断  │      │  ┌────────────▼───────────┐  │  │
│  │  >0.85 回MySQL答案   │      │  │ StrategySelector (LLM) │  │  │
│  │  <=0.85 走RAG       │      │  │   4种策略选1           │  │  │
│  │                     │      │  └────────────┬───────────┘  │  │
│  │  Redis ←→ MySQL     │      │  ┌────────────▼───────────┐  │  │
│  └─────────────────────┘      │  │ VectorStore (Milvus)   │  │  │
│                               │  │ 混合检索 + Reranker精排 │  │  │
│                               │  └────────────┬───────────┘  │  │
│                               │               ▼              │  │
│                               │     LLM 流式生成答案          │  │
│                               └──────────────────────────────┘  │
│                                                                  │
│                    MySQL conversations (对话历史)                 │
└─────────────────────────────────────────────────────────────────┘
```
